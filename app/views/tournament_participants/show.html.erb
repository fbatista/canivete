<%= turbo_frame_tag dom_id(@tournament, dom_id(current_user.player)) do %>
  <%= link_to 'Cancel my registration', [@tournament, @tournament_participant], class: "bg-red-500 text-white px-4 py-2 rounded-md text-sm", data: { turbo_method: :delete } %>
<% end %>
