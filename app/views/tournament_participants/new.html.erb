<%= turbo_frame_tag dom_id(@tournament, dom_id(current_user.player)) do %>
  <%= form_for [@tournament, @tournament_participant], html: { class: 'cvt-form' } do |f| %>
    <div class="actions">
      <%= f.submit 'Register' %>
    </div>
  <% end %>
<% end %>
